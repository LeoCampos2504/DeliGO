// DeliGO Operaciones — Panel personal de un negocio. Operaciones-1D.
// Reutiliza el mismo componente que /mozo/panel/[slug] (mismos params, misma
// autorización server-side vía /api/operativo/mozo/panel/[slug]). La navegación
// se deriva del pathname vía `useOperativoNav`.
export { default } from "@/app/mozo/panel/[slug]/page"
